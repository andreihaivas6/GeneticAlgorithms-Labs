



x = scan(n = 30)

cat(min(x), " & ", max(x), " & ", mean(x), " & ", sd(x))

x = scan(n = 30)
sum(x) # timp



x = scan(n = 5)

cat(min(x), " & ", max(x), " & ", mean(x), " & ", sd(x), " & ")

x = scan(n = 15)

sum(x) * 2 # timp
